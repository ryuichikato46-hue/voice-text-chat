import React from "react";
import ReactDOM from "react-dom/client";
import VoiceTextChat from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <VoiceTextChat />
  </React.StrictMode>
);
